name 'google-authenticator'
org 'amylum'

deps(
  pam: '1.3.0-22'
)

configure_flags(
  prefix: '/usr'
)

cflags
harden

build do
  run './bootstrap.sh'
  configure
  make
  install
end

test do
  run 'google-authenticator --help'
end
